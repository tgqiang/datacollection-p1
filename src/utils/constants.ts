// ==================== DATA QUERY ==================== //

/**
 * The default oldest back-date threshold where data is allowed to be queried from.
 */
export const HISTORICAL_RECORDS_MAX_MONTHS = 3;
/**
 * The default limit to apply on any data query.
 */
export const HISTORICAL_RECORDS_MAX_RECORDS = 100;

/**
 * For standard `WHERE X IN [list]` data queries, this constrains the size of `[list]`.
 */
export const FILTER_ARRAY_MAX_LENGTH = 50;

// ==================== DATA QUERY ==================== //
